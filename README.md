# EvalSys — ระบบประเมินผลวิจัยออนไลน์

![Next.js](https://img.shields.io/badge/Next.js-15-black?logo=next.js)
![TypeScript](https://img.shields.io/badge/TypeScript-5-blue?logo=typescript)
![MongoDB](https://img.shields.io/badge/MongoDB-Atlas-green?logo=mongodb)
![TailwindCSS](https://img.shields.io/badge/TailwindCSS-4-38bdf8?logo=tailwindcss)

ระบบประเมินผลโปรเจกต์วิจัยออนไลน์แบบ Peer Evaluation ที่ช่วยให้อาจารย์ลดภาระการตัดเกรด พร้อมให้นักศึกษามีส่วนร่วมในการประเมินเพื่อนแบบ 360 องศา และแสดงผลเป็นกราฟ Real-time

---

## ✨ ฟีเจอร์หลัก

### สำหรับอาจารย์ (TEACHER)
- **จัดการห้องเรียน** — สร้าง/ลบห้องเรียนพร้อมระบบ Join Code 6 หลักอัตโนมัติ
- **ออกแบบแบบฟอร์มประเมิน** — เพิ่ม/ลบหัวข้อและกำหนดคะแนนเต็มได้ไม่จำกัดข้อ
- **ติดตามความคืบหน้า** — Dashboard แสดง Bar Chart สถานะการประเมินรายกลุ่ม
- **ดูผลคะแนนเชิงลึก** — Radar Chart, คะแนน Min/Max/Avg พร้อมข้อเสนอแนะไม่ระบุตัวตน
- **จัดการสมาชิก** — ดูรายชื่อนักศึกษาทั้งห้อง และลบกลุ่มได้
- **Export ผลลัพธ์** — ดาวน์โหลดผลการประเมินเป็นไฟล์ Excel (CSV) รองรับภาษาไทย

### สำหรับนักศึกษา (STUDENT)
- **เข้าร่วมห้องเรียน** — กรอก Join Code เพื่อเข้าห้อง
- **ลงทะเบียนโปรเจกต์** — สร้างกลุ่ม/งานเดี่ยว ดึงเพื่อนเข้าทีมได้
- **ประเมินเพื่อน** — ให้คะแนนกลุ่มอื่นผ่านแบบฟอร์มที่อาจารย์กำหนด
- **Anonymous Voting** — ระบบออกแบบให้ผลคะแนนไม่ระบุตัวตนผู้ประเมิน
- **ป้องกันการประเมินซ้ำ** — ไม่สามารถส่งแบบประเมินกลุ่มเดิมได้สองครั้ง

---

## 🛠 Tech Stack

| ส่วน | เทคโนโลยี |
|---|---|
| Frontend | Next.js 15, React, TypeScript, Tailwind CSS v4 |
| Backend | Next.js API Routes (App Router) |
| Database | MongoDB Atlas (via Mongoose) |
| Authentication | NextAuth.js v4 (JWT + Credentials Provider) |
| Password Hashing | bcryptjs |
| Charts | Recharts (Bar Chart, Radar Chart) |
| UI/Notifications | Sonner (Toast), Lucide React (Icons) |
| Theme | next-themes (Dark/Light Mode) |

---

## 📁 โครงสร้างโปรเจกต์

```
src/
├── app/
│   ├── api/
│   │   ├── auth/
│   │   │   ├── [...nextauth]/   # NextAuth handler (Login)
│   │   │   └── register/        # API สมัครสมาชิก
│   │   ├── evaluations/         # API ดึงฟอร์ม / ส่งผลประเมิน
│   │   ├── groups/              # API สร้างกลุ่ม
│   │   ├── student/             # API สำหรับฝั่งนักศึกษา (ห้อง, กลุ่ม, ประเมิน)
│   │   ├── teacher/             # API สำหรับฝั่งอาจารย์ (dashboard, results, members)
│   │   └── seed/                # API สำหรับสร้างข้อมูลทดสอบ
│   ├── dashboard/               # หน้าสำหรับอาจารย์
│   │   └── room/[roomId]/       # หน้าจัดการห้องเรียน (Overview, Settings, Members, Results)
│   ├── student/                 # หน้าสำหรับนักศึกษา
│   │   └── room/[roomId]/       # หน้าห้องเรียนของนักศึกษา
│   ├── login/                   # หน้าเข้าสู่ระบบ
│   └── register/                # หน้าสมัครสมาชิก
├── components/
│   ├── Navbar.tsx               # Navbar หลัก (รองรับ Dark Mode, Role Badge, Back Button)
│   ├── AuthProvider.tsx         # SessionProvider wrapper
│   └── ThemeProvider.tsx        # ThemeProvider wrapper
├── lib/
│   └── mongodb.ts               # Mongoose connection handler (cached)
├── middleware.ts                # Route protection & Role-based redirect
├── models/
│   ├── User.ts                  # Model: ผู้ใช้งาน
│   ├── Room.ts                  # Model: ห้องเรียน
│   ├── Group.ts                 # Model: กลุ่ม/โปรเจกต์
│   ├── EvaluationResult.ts      # Model: ผลการประเมิน (ไม่เก็บ evaluatorId)
│   └── EvaluationTrack.ts       # Model: ประวัติการประเมิน (ป้องกันส่งซ้ำ)
└── types/
    └── next-auth.d.ts           # Type augmentation สำหรับ Session
```

---

## 🗄 Data Models

### User
| Field | Type | Description |
|---|---|---|
| name | String | ชื่อผู้ใช้ |
| email | String | อีเมล (unique) |
| password | String | รหัสผ่าน (hashed) |
| role | TEACHER / STUDENT | บทบาท |

### Room
| Field | Type | Description |
|---|---|---|
| name | String | ชื่อห้องเรียน |
| joinCode | String | รหัสเข้าห้อง 6 หลัก (unique) |
| teacherId | ObjectId | อาจารย์เจ้าของห้อง |
| evaluationForm | Mixed (JSON) | แบบฟอร์มประเมิน |
| joinedStudents | ObjectId[] | นักศึกษาที่เข้าร่วมห้อง |

### Group
| Field | Type | Description |
|---|---|---|
| name | String | ชื่อโปรเจกต์ |
| roomId | ObjectId | ห้องที่สังกัด |
| leaderId | ObjectId | หัวหน้ากลุ่ม |
| members | ObjectId[] | สมาชิกกลุ่ม |

### EvaluationResult *(Anonymous)*
| Field | Type | Description |
|---|---|---|
| targetGroupId | ObjectId | กลุ่มที่ถูกประเมิน |
| scores | Mixed (JSON) | ผลคะแนนแต่ละข้อ |
| comment | String | ข้อเสนอแนะ |
> ⚠️ **ไม่มี evaluatorId** — ออกแบบให้ไม่สามารถตรวจสอบย้อนกลับว่าใครให้คะแนนเท่าไหร่

### EvaluationTrack *(สำหรับป้องกันส่งซ้ำ)*
| Field | Type | Description |
|---|---|---|
| evaluatorId | ObjectId | ผู้ประเมิน |
| targetGroupId | ObjectId | กลุ่มที่ถูกประเมิน |

---

## 🚀 การติดตั้งและรันโปรเจกต์

### 1. Clone Repository
```bash
git clone https://github.com/your-username/evalsys.git
cd evalsys
```

### 2. ติดตั้ง Dependencies
```bash
npm install
```

### 3. ตั้งค่า Environment Variables
สร้างไฟล์ `.env.local` ที่ root ของโปรเจกต์:
```env
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/evalsys
NEXTAUTH_SECRET=your-super-secret-key-here
NEXTAUTH_URL=http://localhost:3000
```

### 4. Seed ข้อมูลทดสอบ (ทางเลือก)
หลังจากรันเซิร์ฟเวอร์แล้ว เปิดเบราว์เซอร์และเข้าไปที่:
```
http://localhost:3000/api/seed
```
> ⚠️ คำเตือน: API นี้จะ **ลบข้อมูลทั้งหมด** ใน Database แล้วสร้างข้อมูลตัวอย่างใหม่

### 5. รัน Development Server
```bash
npm run dev
```
เปิดเบราว์เซอร์ที่ `http://localhost:3000`

---

## 👤 บัญชีทดสอบ (หลัง Seed)

รหัสผ่านทุกบัญชี: **123456**

| บทบาท | อีเมล | หมายเหตุ |
|---|---|---|
| อาจารย์ | `somrudee@eval.ac.th` | มีห้องเรียน SWE101 |
| นักศึกษา (หัวหน้ากลุ่ม) | `64010001@eval.ac.th` | มีกลุ่มแล้ว |
| นักศึกษา (งานเดี่ยว) | `64010004@eval.ac.th` | มีกลุ่มแล้ว |
| นักศึกษา (ยังไม่มีกลุ่ม) | `64010005@eval.ac.th` | ทดสอบ flow สมัครกลุ่ม |

**รหัสเข้าห้องเรียนทดสอบ:** `SWE101`

---

## 🔐 ระบบยืนยันตัวตนและสิทธิ์

### กฎการสมัครสมาชิก
- ต้องใช้อีเมลที่ลงท้ายด้วย `@eval.ac.th` เท่านั้น
- ส่วนหน้า `@` เป็น **ตัวเลขล้วน** → บทบาท `STUDENT`
- ส่วนหน้า `@` มี **ตัวอักษรภาษาอังกฤษ** → บทบาท `TEACHER`

### Route Protection (Middleware)
| เส้นทาง | ผู้ที่เข้าได้ |
|---|---|
| `/dashboard/*` | TEACHER เท่านั้น |
| `/student/*` | STUDENT เท่านั้น |
| `/login`, `/register` | Redirect ไปหน้าหลักถ้าล็อกอินแล้ว |

---

## 🏗 Build for Production

```bash
npm run build
npm start
```

---

## 📝 License

This project is for educational purposes.